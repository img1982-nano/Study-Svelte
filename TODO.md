# Todo list

## 優先度：高 (動作の安定・移行)
- [ ] **【重要】静的解析エラーの修正 (bun run check)**
    - [ ] `confetti` の未定義エラーを修正 (ライブラリのインストールとインポート)
    - [ ] `$env/static/public` の参照エラーを修正 (型定義と `.env` の整合性)
    - [ ] Svelte 5 の `$derived` を用いたリアクティブな変数 (`next`, `back`) の定義への修正
- [ ] AIモデルの移行 (OpenRouter/GPT-4o-mini -> Gemini API)
- [ ] UIフレームワークの移行と整理
    - [ ] Flowbite-Svelte から DaisyUI への全面移行
    - [ ] 未使用のUIライブラリ (@skeletonlabs/skeleton, flyonui 等) の削除
- [ ] 画面遷移ロジックの改善 (`window.location.href` -> SvelteKit の `goto` へ)
- [ ] OCR連携の完成 (手書き入力のテキストを採点ロジックに反映させる)
- [ ] バックエンドのOCRワーカーの最適化 (リクエスト毎のterminateを回避)

## 優先度：中 (機能拡張)
- [ ] 学習トップ画面の動的化 (問題集リストをDBから取得するようにする)
- [ ] ポイント機能の実装 (正解時にポイントを加算し、永続化する)
- [ ] 問題集の追加機能 (DBから複数のコレクションを選択できるようにする)
- [ ] 解説生成の精度向上 (プロンプトの調整)
- [ ] 環境変数のバリデーション (起動時に `.env` の存在チェック)

## 優先度：低 (システム・将来)
- [ ] ユーザー認証機能 (Firebase or Auth.js)
- [ ] システム(ObjectStorage/Garage)との連携 (画像保存など)
- [ ] システム(DB)のスキーマ定義の厳密化
- [ ] ダークモード対応 (DaisyUI移行に合わせて)

## 完了済み
- [x] 仮のUIを作る
- [x] 問題ページを作り直す
- [x] API取得の際、原因不明のエラーが起きる問題の解決
- [x] Githubへのバックアップ
- [x] 採点ロジックの基礎実装
- [x] 手書き入力機能の導入
